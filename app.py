import os

# os.system('helm create '+'charts')

# os.system('cp pod.yaml charts/templates/')

# os.system('rm -rf charts/')

# os.system('helm upgrade charts charts/')

# os.system('helm package charts/')

# os.system('helm install deb1 charts-0.1.0.tgz')